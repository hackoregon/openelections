import React from "react";
import BasicsSectionForm from "./BasicsSectionForm";

describe("<BasicsSectionForm/>", () => {
  it("should be defined", () => {
    expect(BasicsSectionForm).toMatchSnapshot();
  });
});
